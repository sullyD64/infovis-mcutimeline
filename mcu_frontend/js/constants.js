export const DEBUG = true;

export const MARGIN = {
  top: window.innerHeight * 0.3,
  right: 50,
  bottom: window.innerHeight * 0.4,
  left: 50,
};
export const SCREENHEIGHT = window.innerHeight - MARGIN.top - MARGIN.bottom;
export const SCREENWIDTH = window.innerWidth - MARGIN.left - MARGIN.right;
